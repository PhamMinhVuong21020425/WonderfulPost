import CompanyLeader from "./components";

export default function Page() {
    return <CompanyLeader />;
}

export const metadata = {
    title: 'Company Leader | Magic Post',
    description: 'Page for company leader',
}